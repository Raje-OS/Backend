package raje.com.rajebackend.content.domain.model.queries;

/**
 * Query object used to request all series sorted by rating in descending order.
 * This class follows the CQRS (Command Query Responsibility Segregation) pattern.
 * No parameters are required to execute this query.
 */
public class GetAllSeriesOrderedByRatingQuery {

    // Default constructor
    public GetAllSeriesOrderedByRatingQuery() {}
}
