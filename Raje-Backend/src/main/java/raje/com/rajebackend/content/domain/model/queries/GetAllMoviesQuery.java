package raje.com.rajebackend.content.domain.model.queries;

/**
 * Query object used to request all movies from the system.
 * This is part of the CQRS (Command Query Responsibility Segregation) pattern.
 * No parameters are needed for this query.
 */
public class GetAllMoviesQuery {

    // Default constructor
    public GetAllMoviesQuery() {}
}
