package raje.com.rajebackend.content.domain.model.queries;

/**
 * Query object used to request all series from the system.
 * Part of the CQRS (Command Query Responsibility Segregation) pattern.
 * This query does not require any input parameters.
 */
public class GetAllSeriesQuery {

    // Default constructor
    public GetAllSeriesQuery() {}
}
