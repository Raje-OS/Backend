package raje.com.rajebackend.content.domain.model.queries;

/**
 * Query object used to request all movies sorted by rating in descending order.
 * Part of the CQRS (Command Query Responsibility Segregation) pattern.
 * This query does not require any input parameters.
 */
public class GetAllMoviesOrderedByRatingQuery {

    // Default constructor
    public GetAllMoviesOrderedByRatingQuery() {}
}
