package raje.com.rajebackend.content.domain.model.queries;

/**
 * Query object used to request all books ordered by their rating in descending order.
 * This class follows the Command Query Responsibility Segregation (CQRS) pattern.
 */
public class GetAllBooksOrderedByRatingQuery {

    // Default constructor. No fields are needed for this query.
    public GetAllBooksOrderedByRatingQuery() {}
}
